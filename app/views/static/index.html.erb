<!DOCTYPE html>
<html lang="pl">
  <head>
    <meta http-equiv="Content-Type" content="text/html; charset=utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title> Strona główna </title>

    <style>
    body {
        background-image: url(<%= asset_path "tlo.png" %>);
        background-repeat: no-repeat;
        background-attachment: fixed;
        background-size: cover;

        

    }
    h1 {
        margin-top: 100px;
        margin-left: 40vw;
        color: white;
    }
    ul {
        margin-left: 45vw;
        margin-top: 44vw;
    }
    li {
        color: white
        text-decoration: none;

    }
    a:link {
        text-decoration: none;
        color: white
    }
    </style>
  </head>

  <body>
    <h1>Strona główna</h1>
    <ul>
        <% @users.each do |user| %>
        <%= link_to user do %>
            <li><%= user.username %> <%= user.last_score %></li>
        <% end %>
    <% end %>
    </ul>
  
  </body>
</html>